name: Build, tests and deploy
on:
  push:
    branches:
      - "main"
      - "release/*"
      - "feature/*"
      - "hotfix/*"

jobs:
  call-build-and-publish:
    uses: igr-santos/github-actions-samples/.github/workflows/build-and-publish.yml@main